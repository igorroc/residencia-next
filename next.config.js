/** @type {import('next').NextConfig} */
const nextConfig = {
	// output: "export",
	// distDir: "dist",
	// images: {
    //     domains: ['residencia.ilrocha.com'],
    // },
}

module.exports = nextConfig
